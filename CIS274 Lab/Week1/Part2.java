/*
1. What does Line 1 do?
It defines a public class named CalculateReceipts, which serves as the main container for the Java code.

2. What is "main" in Line 3?
It is the main method, which is the exact starting point where Java begins running the program.

3. There are braces like "{" and "}" all over. What is their purpose?
They group related blocks of code together, defining where a class or a method begins ({) and ends (}).

4. If Line 8 was System.out.print instead of System.out.println, would anything different happen? Why or why not?
Yes. print leaves the cursor on the same line after outputting text, whereas println automatically moves the cursor to a new line after printing.

5. What does "int" mean in Lines 5 and 6?
int stands for integer. It tells Java to create a variable that holds whole numbers (without decimal points).

6. In Line 8, there are quotation marks around only part of what’s printed, and not around everything. Why is that?
Quotation marks contain exact textR (strings) to display as-is. Words outside quotation marks (like adultFees) are variables, so Java prints their stored values (e.g., 80) instead of their literal names.

7. What does the "+" in Line 8 do?
It combines (concatenates) text strings and variable values together into a single message for display.

8. What does Line 9 do? It seems to be a stray piece of something.
It is a continuation of the System.out.println statement from the line above, split across multiple lines to keep the code readable.
*/